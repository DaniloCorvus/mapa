<?php

namespace App\Services;

use App\Repositories\ArchivoRepository;

class ArchivoService
{

    public function update()
    {

        // $archivo->nombre = $request->title;
        // $archivo->descripcion = $request->description;
        // if ($request->file('pdf')) {
        //     \File::delete(public_path('/pdfs/' . $archivo->pdf_path));
        //     $filename = '_' . time() . '_' . $request->file('pdf')->getClientOriginalName();
        //     $request->file('pdf')->move(public_path('/pdfs'), $filename);
        //     $archivo->pdf_path = $filename;
        // }
    }
}
